describe('onboarding', () => {
  it('imports contacts', () => false);
  it('does not duplicate contacts', () => false);
  it('lets you merge conflicts', () => false);
  it('lets you skip conflicts', () => false);
  it('show details and image in people box', () => false);
});

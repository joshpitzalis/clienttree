describe.skip('cms', () => {
  it('reorder people in a stage', () => {
    cy.visit('/');
  });

  it('move people between stages', () => {
    cy.visit('/');
  });

  it('move  stages around', () => {
    cy.visit('/');
  });

  it('add people to a stage', () => {
    cy.visit('/');
  });

  it('update someone in a stage', () => {
    cy.visit('/');
  });

  it('delete someone from  a stage', () => {
    cy.visit('/');
  });

  it('delete someone from  any stage, not just the first one', () => {
    cy.visit('/');
  });

  it('add a resource to a stage', () => {});

  it('delete a resource from a stage', () => {});

  it('add a new stage', () => {});

  it('update a stage', () => {});

  it('delete a stage', () => {});
});

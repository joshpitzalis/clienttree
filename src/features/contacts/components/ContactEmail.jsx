import React from 'react';

export const ContactEmail = () => {
  const handleContacEmail = () => {
    // trigger cloud function
  };

  return (
    <button type="button" onClick={handleContacEmail}>
      Send Contact Email
    </button>
  );
};

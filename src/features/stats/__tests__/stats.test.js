describe('stats box', () => {
  test('box hides till you enter an income goal', () => false);
  xtest('modal closes to incomplete if no goal', () => {});
  xtest('modal closes to complete if goal is saved', () => {});
  xtest('goes back to incomplete if a goal is removed', () => {});
});
